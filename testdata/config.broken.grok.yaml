---
textExtractor:
  "text/plain":
    type: builtin
grokPatterns:
  NUMBER: '[0-9]+'

commonTemplate: ""

fileDescriptions:
  broken:
    patterns:
      - "%{NONEXISTENT:field}"
    output: "{{ .Grok.field }}"
