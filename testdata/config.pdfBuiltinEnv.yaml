---
textExtractor:
  "application/pdf":
    type: builtin

env:
  - COMPANY

grokPatterns:
  INVOICENO: '[A-Z0-9-]+'

fileDescriptions:
  invoice:
    patterns:
      - "Invoice No: %{INVOICENO:invoiceNumber}"
    output: "Invoice {{ .Grok.invoiceNumber }} from {{ .Env.COMPANY }}\n"
